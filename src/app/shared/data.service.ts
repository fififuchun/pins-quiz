import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class DataService {
  constructor() {}

  import() {
    // ファイルを読み込んで結果をreturn

    // return "test"

    // return Math.floor(Math.random() * 151) - 1;
  }
}
